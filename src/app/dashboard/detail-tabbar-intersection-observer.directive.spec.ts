import { DetailTabbarIntersectionObserverDirective } from './detail-tabbar-intersection-observer.directive';

describe('DetailTabbarIntersectionObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailTabbarIntersectionObserverDirective();
    expect(directive).toBeTruthy();
  });
});
