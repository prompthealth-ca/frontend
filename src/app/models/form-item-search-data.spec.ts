import { FormItemSearchData } from './form-item-search-data';

describe('FormItemSearchData', () => {
  it('should create an instance', () => {
    expect(new FormItemSearchData()).toBeTruthy();
  });
});
