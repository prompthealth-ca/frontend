import { FormItemTextfieldOption } from './form-item-textfield-option';

describe('FormItemTextfieldOption', () => {
  it('should create an instance', () => {
    expect(new FormItemTextfieldOption()).toBeTruthy();
  });
});
