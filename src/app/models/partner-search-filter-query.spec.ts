import { PartnerSearchFilterQuery } from './partner-search-filter-query';

describe('PartnerSearchFilterQuery', () => {
  it('should create an instance', () => {
    expect(new PartnerSearchFilterQuery()).toBeTruthy();
  });
});
