import { StripeCheckoutData } from './stripe-checkout-data';

describe('StripeCheckoutData', () => {
  it('should create an instance', () => {
    expect(new StripeCheckoutData()).toBeTruthy();
  });
});
